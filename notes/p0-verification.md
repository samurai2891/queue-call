# P0 UI/UX改善 ブラウザ確認結果

## P0-2: BoardDisplay空状態のQRコード表示
- 店名「テスト」がヘッダーに表示
- QRコードエリアが表示（プレビューモードでは実際のQRコード画像は表示されない可能性あり）
- 「スマホでかんたん受付」テキスト表示
- 「QRコードを読み取って受付」説明テキスト表示
- 背景は薄い青のグラデーション → OK

## P0-3: BoardDisplay呼び出し番号の巨大化
- 待ち客がいない状態ではQRコード案内が表示される → OK
- 呼び出し中の番号表示は実際にチケットを作成して確認が必要

## P0-4: Staff画面の改善
- 「呼び出し(0)」ボタンが画面最上部に大きく表示 → OK
- ステータスカード（待機中/呼び出し中/到着済み）がコンパクトなインライン表示 → OK
- 「手動チケット追加」が折りたたみ式に変更 → OK
- 受付ステータスがインラインで表示 → OK
- 順番調整モードがコンパクトに表示 → OK
- 待ちリストが空状態で適切なメッセージ表示 → OK
