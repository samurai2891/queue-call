# Skill: Tooling & Workflows

## Applies to
- `scripts/**`
- build config files at repo root

## Expectations
- Keep scripts idempotent and safe for local dev.
- Document required environment variables in the script headers if needed.
- Avoid introducing new runtime dependencies without updating package.json.
